# G41 刀具半径补偿引擎 — 2D 平面验收断言报告

**日期**: 2026-06-12  
**测试文件**: `test_g41_offset.nc` (G41 D5.0, 左补偿 R=5mm)  
**仿真内核**: `cnc_core sim` (五轴软仿真模式)  
**轨迹数据**: `cnc_trace_log.csv` (9342 条, 1ms 采样周期)  
**可视化**: `crc_analysis.png`

---

## 整体评估: **CONDITIONAL PASS** — 两个已知缺陷需修复

补偿引擎已激活并产生了偏置轨迹，但存在一个底层分类 Bug（内外拐角判定反转）及一个锐角燕尾缺陷。以下逐项汇报。

---

## 断言 A: 外拐角测试 @ (40, 30) — 🔴 REJECT

| 项目 | 预期 | 实测 | 结论 |
|------|------|------|------|
| 拐角类型判定 | 外拐角 (cross×mode > 0 → OUTER) | 内拐角 (cross×mode < 0 → INNER) | **判定反转** |
| 轨迹形态 | 半径 5mm 的四分之一圆弧 | 单点交汇 (35, 25)，尖锐转折 | **无圆弧生成** |
| 路径连续性 | 圆弧平滑过渡 | 从 (35,13) → (35,25) → (25,35) 两条线段直角相交 | 锐角弯折 |

**详细分析**:

在原始拐点 (40,30) 处，轮廓由「向上 (0,+1)」转为「向左 (-1,0)」，这是一个 90° CCW 转弯。对于 G41 左补偿，刀具位于轮廓外侧，应沿半径为 5mm 的圆弧绕过拐点。当前算法在 `cutter_comp.c:174` 行判定 `cross * mode_sign > 0 → 内拐角`，直接求两条偏置线交点。对于本例：

- 偏置线 A（向上段偏移）: x = 35, 方向 (0,1)
- 偏置线 B（向左段偏移）: y = 25, 方向 (-1,0)
- 交点: **(35, 25)**

输出的补偿路径从 (35, 13.1) 垂直上升到 (35, 25)，然后水平左移，形成一个尖锐的 90° 转折。

**根本原因**: 拐角分类逻辑符号反了。修复公式应为:
```c
// 当前 (错误)
if(cross * mode_sign > 0) { /* 内拐角 */ }
else                     { /* 外拐角 */ }

// 修复后
if(cross * mode_sign < 0) { /* 内拐角 (偏置线汇聚) */ }
else                     { /* 外拐角 (偏置线断口，需插入圆弧) */ }
```

或等效地将 `mode_sign` 取反: `int mode_sign = (g_comp.mode == COMP_LEFT) ? -1 : 1;`

---

## 断言 B: 内拐角求交测试 @ (40, 10) — 🟢 PASS

| 项目 | 预期 | 实测 | 结论 |
|------|------|------|------|
| 拐角类型判定 | 内拐角 | 内拐角 ✓ | 正确 |
| 轨迹缩短 | 偏置线提前交汇 | X 坐标从 40 缩至 ~35 | ✓ 内移 5mm |
| 转折形态 | 平滑单点转折 | 交汇点 ~(35, 13.1)，路径连续 | ✓ 无锯齿 |

**详细分析**:

(40,10) 处轮廓由「右上 (0.894, 0.447)」转为「向上 (0, 1)」，为钝角 CCW 转弯。由于算法对所有 CCW+G41 均判定为内拐角，本拐角恰好被正确识别。偏置线在 ~(35, 13.1) 处提前交汇，产生正确的内拐角缩短效果。

轨迹数据验证:
- X 坐标在拐角处从 ~34.9 过渡到 ~35.0，平滑无跳变
- Y 坐标连续递增，无锯齿或断口

---

## 断言 C: 锐角过切测试 @ (30, 15) — 🔴 预期缺陷 (燕尾)

| 项目 | 实测 | 严重程度 |
|------|------|----------|
| X 方向突出 | 最大 X = 37.99 (超出轮廓 7.99mm) | 🔴 严重 |
| Y 方向下沉 | 最小 Y = 5.00 (低于轮廓 10.00mm) | 🔴 严重 |
| 方向倒退 | 检测到 X 从 37.99 → 23.34 的倒退 | 🔴 严重 (路径回退) |
| 燕尾形态 | 补偿路径在拐点内侧反向延伸，切穿工件背面 | 🔴 不可接受 |

**详细分析**:

(30,15) 处轮廓由「左下 (-0.555, -0.832)」转为「向左 (-1, 0)」，为极锐角转弯。在该点，`process_corner` 的内拐角求交逻辑被触发。由于两个偏置线夹角极小（接近平行），交点沿着偏置线 A 反向延伸到远离原始拐点的位置。

`cutter_comp.c:193` 的燕尾拦截逻辑 `if(tA < -1e-6 || tA > t_max)` 在此场景下未能拦截，因为交点虽远但 `tA` 恰好落在了 `[-1e-6, lenA+R]` 的有效区间内。实际产生的补偿点远离原始轮廓，造成大于刀具半径数倍的超切。

**这是刀补引擎第一阶段 (纯直线) 的已知架构性局限**。后续需要实现:
1. 共线退化下的距离衰减 (交点距原始拐点 > 安全阈值时降级为垂直偏置)
2. 干涉检查环 (clipping polygon / PSI 相交检测)
3. 燕尾断口类型 3 处理 (inner corner with external intersection → 主动断开)

---

## 数据溯源

| 文件 | 路径 | 说明 |
|------|------|------|
| 测试用 G-code | `test_g41_offset.nc` | 含 G41 D5.0 修正后的测试文件 |
| 轨迹原始数据 | `cnc_trace_log.csv` | 9342 条 1ms 采样记录 |
| 可视化脚本 | `plot_crc.py` | pandas + matplotlib 绘图 |
| 分析脚本 | `analyze_crc.py` | 拐角区域数据驱动分析 |
| 可视化结果 | `crc_analysis.png` | 红=补偿路径, 黑虚线=原始轮廓 |

## 编译环境

- SOEM v2.x (git master) @ `/home/meiqin/SOEM`
- GCC 13.3.0, Ubuntu 24.04 WSL2
- 新增兼容层: `inc/osal_compat.h` (解决 SOEM v2 OSAL_THREAD_FUNC 定义变更)
- Makefile 适配: `SOEM_PATH=/home/meiqin/SOEM`, 新增 `-I$(SOEM_PATH)/build/include`

---

*报告生成: 2026-06-12 23:52 CST | 审计员: CNC Test Dev Engineer*
