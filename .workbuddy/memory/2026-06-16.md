# 2026-06-16 — CNC 项目工作日志

## 4 攻击面安全审计：拓扑锁定 / SNR 屏蔽 / S 曲线速度解析 / 跨周期状态
- **攻击面1 Hard-RT浮点**: v_current 全用乘加无除零; NaN 由 planner 熔断隔离; 负速钳零; s 单调性由构造保证; FMA 增精度非减 ✓
- **攻击面2 SNR屏蔽**: step_size_mm 受 BSpline_Configure 钳制 ≥ 0.01; noise_floor=0.0015mm 安全; 纯旋转轴半径=0 按度计算安全 ✓
- **攻击面3 内存模型**: v_current 仅 RT 线程内读写; just_loaded_seg 每周期仅 1 段 (if 非 while) 无穿透漏记 ✓
- **攻击面4 拓扑锁**: is_fillet 仅跳过 planner_fillet_preprocess; calculate_junction_speed 对 G94 still effective; G93 is_g93_strict 跳过 junction 但 B-Spline 输出切线连续 ✓
- **压榨建议**: 8路 if-else 改 phase-indexed switch; T4 无需重算 v_now=v3; 7 个 _T 缓存可削减
