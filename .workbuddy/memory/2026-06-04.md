# 2026-06-04 — CNC 项目工作日志

## G93 倒数时间进给审计 (PASS with 1 WARNING)
- **Kill List #1 除零/负数**: F <= 1e-6 → 拒绝 ✓; v_req < 1e-6 → 钳制 1e-6 ✓
- **Kill List #2 G00 穿透**: motion_mode==0 优先级最高，不经 G93 路径 ✓
- **Kill List #3 旋转轴黑洞**: equivalent_radius==0 时不经转换，但距离≠0 不触发除零 ✓; v_req clamp 兜底 ✓
- **Kill List #4 圆弧打碎**: Arc_Length / T_sec 统一切向速度 → 所有子段共用 ✓ (非 T_sec/num_segments)
- **⚠️ W-1**: equivalent_radius 未配置时静默使用 raw 角度 → 距离/速度偏大 (建议加 printf 警告)
- **⚠️ W-2**: I/J/K 无条件设 has_move=1 → G01 模态下 I/J 会生成零距段入队 (性能噪声，非致命)
- **裁决**: PASS — 无致命缺陷。建议修复 W-1 增加开发者可见警告

## 动态轴映射与 M 代码参数扩展审计 (审查通过)
- **Kill List #1 查表越界**: SMC_ConfigAxisTopology 有 A-Z 边界检查 ✓; 动态映射双重检查 (letter + idx bounds) ✓
- **Kill List #2 圆弧死锁**: generate_arc_trajectory 查表后 idx<0 → return -1 ✓ (激光切管机仅 X/A 映射时 G17 触发拦截)
- **Kill List #3 参数泄露**: p_value/q_value/r_value 局部变量逐行清零 ✓; api_push_mcode 签名扩展为 5 参数 ✓
- **Kill List #4 映射防线**: axis_sys_init 显式 for(i=0;i<26;i++) g_axis_map[i]=-1 ✓; main.c 调用顺序 init → ConfigTopology ✓
- **裁决**: 审查通过 — 四项 Kill List 全部 PASS

## "动态轴映射与算法解绑"二次审计 (安全防线完整，允许合并)
- **裁决**: 安全防线完整，允许合并

## G43.4 RTCP 运动学逆解审计 (通过)
- **Kill List #1 度/弧度转换**: DEG2RAD=PI/180, sin/cos前统一转换 ✓; A/C轴均独立乘DEG2RAD ✓
- **Kill List #2 查表安全**: idx_x/y/z/a/c 五轴查表: idx<0→return ✓; 未映射回退安全 ✓
- **Kill List #3 状态污染**: current_pos=target_pos (G代码坐标, 非物理机器坐标) ✓
- **Kill List #4 IK矩阵**: R_Z(C)*R_X(A) 手工展开验证通过 ✓; 测试点(10,10,5) A=45° C=30° → (6.893,8.062,10.605) 物理合理 ✓
- **裁决**: 四项Kill List全部PASS — 矩阵符号/度弧度/状态链/查表防线均正确

## N 段动态前瞻缓冲与安全释放审计 (审查通过，允许合并)
- **Kill List #1 尾部死锁**: force_flush→eff_last 全员释放, 双路 watchdog (50ms/2s) ✓; eff_last 尾段 v_end=0 确保刹停 ✓
- **Kill List #2 环队越界**: 统一 `(cursor +/- 1 + QUEUE_SIZE) % QUEUE_SIZE` 模式, 无裸 ++ 或 -- ✓
- **Kill List #3 M码透传**: 安全释放扫描遇 CMD_TYPE_MCODE → break → 屏障前段可释放, 后段隔离 ✓
- **Kill List #4 参数同步**: 每轮初始化 v_start/v_end=0 + 全量反向/正向扫描 + recompute_scurve_profile ✓
- **增量改进**: 废弃 plan_count==1/2/3 硬编码 → 统一反向+正向; LOOKAHEAD_MAX_DEPTH=200 分批; eff_last 替代 head-1
- **裁决**: 四项 Kill List 全部 PASS — 死锁/越界/透传/同步 四道防线完整
- **对比上次审计的增量修复**:
  - `toupper((unsigned char)axis_name[0])` — UB 修复 ✓
  - `axis_name == NULL || axis_name[0] == '\0'` — 空指针/空串 拦截 ✓
  - `idx >= AXIS_NUM` — generate_arc_trajectory 补充上界检查 ✓
  - `ax1=-1, ax2=-1` — 默认初始化防未写入 ✓
- **Kill List #1 字母冲突**: switch-case 优先于 default，G/F/I/J/K/M/P/Q/R/S 永远不会被当做运动轴 ✓
- **Kill List #2 圆弧维度**: 查表→双边界→array[映射索引]，X=3 时 next_pos[3] 精确取格 ✓
- **Kill List #3 字符串安全**: NULL/空串/unsigned char/边界检查 四层防护 ✓
- **Kill List #4 映射初始化**: axis_sys_init → for 26 格全 -1 → ConfigTopology 顺序正确 ✓
