# 2026-06-22 工作日志

## 红方安全审计 — 混合并发模型 4 攻击面完整审查

**判定: CONDITIONAL PASS** (F×2 + H×1 + 数据竞争×2)

### 审计范围
- `src/planner.c` — 完整 (811行): 7段S曲线、Fillet内存平移、前瞻引擎、Try-Lock入口
- `src/axis_ctrl.c` — 完整 (1121行): 生产者入队(spin-wait)、api_flush_planner、api_push_mcode
- `src/ecat_core.c` — 完整 (1062行): RT线程(SCHED_FIFO 99)、看门狗、段消费
- `src/gcode_parser.c` — parser线程结束逻辑
- `inc/global_def.h` — 全局变量声明
- `inc/axis_cfg.h` — CommandQueue_t Cache Line布局

### 关键发现

**攻击面 1 (Try-Lock 饥饿) → CONDITIONAL PASS → 已修复:**
- api_flush_planner 使用 try-lock，竞争失败时静默返回
- 最坏情况: 最后一段挂起 0~50ms 待看门狗兜底
- root cause: 无 spin-wait 重试机制
- **修复**: 重构 planner_recalculate 为 try-lock入口 + planner_recalculate_locked 持锁核心;
  api_flush_planner 改用 spin-wait + cpu_relax 直接调核心

**攻击面 2 (优先级反转) → PASS:**
- RT 线程完全不接触 queue_spinlock → 免疫优先级反转
- 但 spin-wait 循环缺 cpu_relax() → H级性能隐患
- **修复**: 3处 spin-wait (push_traj, push_mcode, api_flush) 均加入
  x86 PAUSE / ARM YIELD 指令

**攻击面 3 (Fillet Torn RT Read) → PASS:**
- 4层防护: is_ready门控 + 值不变性 + x86 TSO + Release/Acquire
- 移位范围与RT消费位置空间隔离,物理不相交

**攻击面 4 (False Sharing) → PASS:**
- write_head/read_tail/queue_spinlock 各自 _Alignas(64) 独占cache line

**额外发现 (无需修改):**
- g_parser_ctrl.is_running 非原子多线程访问 → 数据竞争 (x86安全,ARM危险)
- planner内 read_tail 用 relaxed 读 → ARM移植隐患

### 修改文件清单
1. `src/planner.c` — 拆分 planner_recalculate → planner_recalculate_locked + planner_recalculate(try-lock wrapper)
2. `inc/planner.h` — 新增 planner_recalculate_locked 声明
3. `src/axis_ctrl.c` — api_flush_planner spin-wait; 3处 spin-wait + cpu_relax

---

## 第二轮审计：非线性动力学 3 攻击面 (16:41)

**攻击面 1 (夹角极值奇点) → PASS:**
- cos≥0.999 / cos≤-0.999 两闸门跳过 acos/cos(α/2)/cbrt
- denom≤1e-6 clamp 防除零
- cbrt(非负数) 始终有限
- NaN/Inf 熔断双保险 (行348)

**攻击面 2 (O(N²) 性能雪崩) → PASS:**
- LOOKAHEAD_HARD_CAP=512 硬截所有扫描
- 所有7步迭代上界 < 7000 次 循环
- 总耗时约 42µs << 1ms

**攻击面 3 (短段阶跃 Jerk) → CONDITIONAL PASS (W级):**
- 短段物理钳制 √(a·L)·0.7 — 有效但被后向v_start覆盖
- solve_max_vstart_scurve 在 lo==hi 退化: 距离约束被静默绕过
- Step 6 距离熔断兜底有效
- 建议加固: solve_max_vstart_scurve 入口增加 lo==hi 退化分支
