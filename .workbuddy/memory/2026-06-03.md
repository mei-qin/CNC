# 2026-06-03 — CNC 项目工作日志

## 红军审计：F-2 修复审查 + 专项漏洞核查
- **蓝方回执**: 采纳 F-2 (熔断反向传播后前驱段 T1~T7 未重算)，拒绝 F-1 (DEFAULT_JERK_MS3 除零) 和 F-3 (safe_release_head 默认值)
- **F-2 修复审查**: `recompute_scurve_profile()` 函数实现正确，消除了熔断→前驱段 S 曲线重算的时间窗口。但发现 2 个残留问题:
  - H-1: `recompute_scurve_profile` 内 `v_target` 读取的是 main loop 可能已钳制的值 (安全但次优)
  - H-2: 二级反向传播缺失 (recompute 熔断后未更新前驱-1 的 v_end)
- **专项核查**: 发现 2 个致命缺陷需修复:
  - ① 尾部残留卡死: watchdog 的 `count < 3` 条件导致 3+ 段残留时无保护；parser 异常退出时也不触发
  - ② 微小段时间截断: planner 生成 T_total ∈ (0, 0.5]ms 的段, RT 线程 `if(seg.T_total > 0.5)` 阈值拒绝 → 位置静默丢失
  - 修复建议: (①) 增加 `count >= 3` 的长时间超时保护；(②) planner 输出前强制 T_total ≥ CYCLE_TIME_NS

## 蓝方修复核查 (PASS)
- **修复 1 — 双路径看门狗 (ecat_core.c:650-688)**:
  - 路径 A: `count > 0 && !is_running` — 去除 count<3 限制，50ms 强制释放 ✓
  - 路径 B: `count > 0` — 同时追踪 head+tail，2s 兜底 (STUCK_THRESHOLD=200) ✓
  - 残留盲区: 解析器持续推段但所有段 accum < S_stop 时，队列满前不触发；延迟可控
  - 预存问题: RT 线程调用 planner_recalculate(1) 的优先级反转风险 (非本次引入)
- **修复 2 — 微段快照 (ecat_core.c:358-366)**:
  - T_total ≤ 0.5ms → 直接 `current_pos = target_pos` ✓
  - 位置链: 上段终点 → 微段 target → 下段 start_pos 三环完整 ✓
  - PDO 同周期输出 (line 489 current_cmd_pos = current_pos → line 536 PDO write) ✓
- **裁决**: 两个修复均正确实现，无阻塞项，可以合并

## G 代码模态与解析逻辑审计 (REJECT)
- **Kill List 结果**:
  - #1 同组模态覆盖: ✅ PASS (左→右解析天然满足后者生效)
  - #2 I/J/K 模态污染: ✅ I/J/K 局部变量，不在 GCodeState_t 中
  - #3 幽灵零距离段: ✅ PASS (has_move 门控正确)
  - #4 开机安全: ✅ PASS (motion_mode=1 G01, 防止开机飞车)
- **致命缺陷 F-4**: 零半径圆弧 + 无声返回 + current_pos 前移
  - 场景: G02 模态传承 + 无 I/J → radius=0 → generate_arc return 0 → current_pos=target_pos
  - 后果: 10mm 位置漂移 → 下一条 G01 从虚假起点出发 → 撞机风险
  - 修复: 在 generate_arc_trajectory 调用前预检 hypot(off_1st, off_2nd) < 0.001 → return 0 跳过
- **警告**: W-1 (Z轴缺 AXIS_NUM 保护), W-2 (G码 double == 比较)
- **裁决**: REJECT — F-4 必须修复
## G 代码解析器 F-4 修复复查 (PASS)
- **修复位置**: generate_arc_trajectory (gcode_parser.c:227-237)
- **机制**: radius<0.001 时不再 silent return 0，而是：
  - 起终点重合(dist_sq<1e-12) → return 0 (无操作)
  - 起终点不同 → 退化为直线: `api_push_trajectory(end_pos, feedrate, acc, dec)`
- **验证**: 位置链 (machine_target → RT实际终点 → current_pos) 三环闭合 ✓
