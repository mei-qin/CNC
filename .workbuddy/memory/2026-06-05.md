# 2026-06-05 — CNC 项目工作日志

## CAM 文件兼容性审查
- **来源**: SmartCAM, 5轴RTCP叶轮加工刀路 (X/Y/Z/B/C, ~400段)
- **G43.4 RTCP**: 识别 ✓, parsing 正常
- **F1200/F2000 进给**: G94 模式运作正常

## "注释过滤与 G53 非模态解析"审计 (安全防线完整，允许合并)
- **Kill List #1 G53 全局污染**: is_G53_this_block 局部变量，从未赋值 g_coord_mgr.current_coord ✓
- **Kill List #2 逻辑坐标反推**: target_pos = machine_target - work_offset 正确定向 ✓
- **Kill List #3 注释死循环**: while(*p!=')' && *p!='\0') 双重保护 + continue 后 '\0' break ✓
- **Kill List #4 无坐标穿透**: G53 G00 → has_move=0 → 不生成轨迹 ✓
- **增量功能**: `(` 注释跳过 + `;`/`%`/`\n`/`\r` 行终止 + G91=0→G53 强制 G90
- **裁决**: 四项 Kill List 全部 PASS — 局部标志位隔离，坐标反推链闭合，注释 OOB 防护到位
